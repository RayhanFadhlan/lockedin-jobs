Options -Indexes

RewriteEngine On

# Stop processing if already in the /public directory
RewriteRule ^public/ - [L]

# Stop processing if in the /storage directory
RewriteRule ^storage/ - [L]

# Push every other request to index.php
RewriteRule ^(.+)$ index.php [QSA]